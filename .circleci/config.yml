version: 2
jobs:
  build:
    working_directory: ~/comunicator
    docker:
    - image: circleci/python:3.6.4
    steps:
      -checkout
      -run:
        command: |
          sudo pip install pipenv
          pipenv install sha3
          pipenv run python test.py test
